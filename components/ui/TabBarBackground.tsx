/**
 * TabBarBackground
 * -------------
 * This handy wee component draws a custom background for the tab bar.
 * Lets you style the tab bar to match the app's theme, with gradients or colours as you fancy.
 * Used in the TabLayout to keep the bottom of the app looking tidy.
 * 
 */

// This is a shim for web and Android where the tab bar is generally opaque.
export default undefined;

export function useBottomTabOverflow() {
  return 0;
}
